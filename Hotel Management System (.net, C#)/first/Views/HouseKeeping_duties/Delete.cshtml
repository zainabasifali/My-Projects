@model first.Models.HouseKeeping_duties

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Delete Housekeeping Duty</h2>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-danger text-white">
            <h5 class="mb-0">Confirm Delete</h5>
        </div>
        <div class="card-body">
            <p>Are you sure you want to delete this housekeeping duty record?</p>
            <dl class="row">
                <dt class="col-sm-3 fw-semibold">
                    @Html.DisplayNameFor(model => model.status)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.status)
                </dd>

                <dt class="col-sm-3 fw-semibold">
                    @Html.DisplayNameFor(model => model.issue_reported)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.issue_reported)
                </dd>

                <dt class="col-sm-3 fw-semibold">
                    @Html.DisplayNameFor(model => model.date)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.date)
                </dd>

                <dt class="col-sm-3 fw-semibold">
                    Assigned Staff
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.user.name)
                </dd>

                <dt class="col-sm-3 fw-semibold">
                    Room Number
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.room.number)
                </dd>
            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.id)

                <div class="mt-3">
                    <input type="submit" value="Delete" class="btn btn-danger rounded" />
                    @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-secondary rounded ms-2" })
                </div>
            }
        </div>
    </div>

    <div class="modal fade" id="deleteConfirmationModal" tabindex="-1" aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="deleteConfirmationModalLabel">Confirm Delete</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    Are you sure you want to delete the duty assigned to: <strong>@Html.DisplayFor(model => model.user.name)</strong> for room <strong>@Html.DisplayFor(model => model.room.number)</strong>?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary rounded" data-bs-dismiss="modal">Cancel</button>
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.id)
                        <input type="submit" value="Delete" class="btn btn-danger rounded" />
                    }
                </div>
            </div>
        </div>
    </div>
</div>
